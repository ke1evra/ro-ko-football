# ===============================================================
# =           Переменные окружения для Production               =
# ===============================================================
# Скопируйте этот файл в .env и замените значения-плейсхолдеры
# cp .env.example .env

# --- База данных MongoDB ---
# Замените <user>, <password> и <dbname> на ваши реальные данные.
# Для развертывания на сервере с Ubuntu используйте 127.0.0.1.
# Для локальной разработки ВНЕ Docker используйте localhost через переменную ниже.
# Для локальной разработки ВНУТРИ Docker или если приложение в контейнере, используйте host.docker.internal.
DATABASE_URI=mongodb://syncadmin:syncadminsyncadmin123qQ!@127.0.0.1:27017/payload?authSource=admin
# Локальная разработка вне Docker (используется только при NODE_ENV=development):
MONGODB_URI_LOCAL=mongodb://localhost:27017/payload

# --- Секретный ключ Payload ---
# Сгенерируйте новый ключ командой: openssl rand -hex 32
PAYLOAD_SECRET=y38fb08a2c4554593175ebb2f37fe8d44898eaa15da89f2be560b0f85d1208fd2

# --- Настройки приложения ---
# Установите 'production' для сервера
NODE_ENV=production
# Укажите ваш домен
APP_URL="http://rocoscore.ru"


# ===============================================================
# =           Опциональные переменные                           =
# ===============================================================

# --- Конфигурация Email (Resend) ---
RESEND_API_KEY="re_xxxxxxxx"
EMAIL_FROM="noreply@rocoscore.ru"

# --- LiveScore API ---
LIVESCORE_KEY=JFSoWhkq1FIky8SS
LIVESCORE_SECRET=qbjc7aeuFAJo1E5KjX8fetGg9oAIxtBY
LIVESCORE_API_BASE=https://livescore-api.com/api-client

# --- Vercel Blob (S3-совместимое хранилище) ---
# BLOB_READ_WRITE_TOKEN="<your_blob_token>"

# --- Cloudflare R2 (S3-совместимое хранилище) ---
# R2_ACCESS_KEY_ID="<your_r2_access_key>"
# R2_SECRET_ACCESS_KEY="<your_r2_secret_key>"
# R2_BUCKET="<your_r2_bucket>"
# R2_ENDPOINT="<your_r2_endpoint>"
